# To Do List

Don't go to cosine similarity mode when a new word is added, because the mouse isn't in the right position to support drawing the leader lines, so it's useless.

When the user enters an unknown word in a semantic dimension word list box, the error message appears at the bottom of the window and is easy to miss. Find a way to display it more prominently.

Add support for switching between several embeddings.

Add a subpanel for "Outlier Mode" where the user can enter four words and we compute all the dot products, find the outlier, and display the words as nodes in the plane with spring forces determining their layout.

